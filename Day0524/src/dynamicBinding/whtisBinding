intstanceof -> 변수가 가리키는 객체의 실제 타입을 알고 싶을 때 사용\
fianl - 상속을 시킬 수 없는 클래스 ( 보안상의 이유로 사용)
ex) 
class Baduk{
	enum BadukPlayer{ WHITE, BLACK }  // enum: 열거를 의미, 인덱스로 처리한다.
	final BadukPlayer getPlayer(){
		return BadukPlayer.BLACK;
	}
}
