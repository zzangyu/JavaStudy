package com.test;

public class TestExam {

	public static void main(String[] args) {
		//System.out.println("집에 갈래.."); //ln(line feed)은 다음 라인으로 바꿔라의 약자임, 엔터를 친거랑 똑같음
	    //System.out.print("안녕히계세요."); //괄호 안의 내용을 출력한 후 한 행을 띄지 않고 유지
		//System.out.printf("100"); //f(format)형식을 갖춰라. 숫자면 숫자, 문자면 문자, 숫자 100이 아니라 글자 100(일영영)임
		//System.out.printf("%d", 100);//숫자 100을 의미, 서식 %d=숫자를 의미
		//System.out.printf("100+100");//글자로 인식했기 때문에 계산이 안된다.
		//System.out.printf("%d", 100+100); //%d(decimal)서식을 사용했기 때문에 계산이 된다.
		//System.out.printf("\n");//강제로 행이 넘어가게 한다.
		//System.out.printf("%d", 100, 200); //%d는 하나, 숫자는 2개이지만 에러는 안뜬다.
		//System.out.printf("%d %d", 100); //%d가 두개라 자리기 하나 남기 때문에 에러가 뜬다.
		//System.out.printf("%d/%d=%f", 100, 200, 0.5); //0.5는 실수이기 때문에 %f를 쓴다.
		//콘솔에다가 출력해라  //요놈은 주석문이다~! 설명 달아놓는 곳, ctrl+f11이 바로 프린트하는 커맨드
	   /*/*은 저 밑에까지 쓸 수 있다.
	     * byte(1바이트) short(2바이트) char (문자라는 뜻2바이트) int(4바이트) long(8바이트) float(4바이트) double(8바이트)
	     * long보다 double이 더 큼(실수형이라)	      2^64 까지 가능
	     * f(float): 소숫점 6번째 자리까지 나타낸다. 0.5면 0.500000
	     * "%7.1f"면 소수점 첫번째 자리까지만 출력 두번째 자리에서 반올림
	     * "%7.3f"면 소수점 세번째 자리까지만 출력 세번째 자리가 없으면 0을 채운다.
	     * %d,%x,%o 정수, %f 실수(소수점이 있는 수), %c 문자 반드시 한 글자이고 작은 따옴표'', %s 문자 한 글자 이상 큰 따옴표""
	     * 역슬레시 물어보기 \\\\\\->\\\   
		 * System.out.println(args[0]);//run->run configurations->argument부분을 채워준다.
	     * https://marketplace.eclipse.org/content/indent-guide 밑에 결과창 받는 곳
		 *
		 * int 정수만 들어감. 소수점x 음수o
		 * 
		 * 소수점이 있는 실수형
		 * float    실수형     4바이트
		 * double 큰 실수형  8바이트
		 * 
		 * 문자형 데이터 형식 - 아스키코드(컴퓨터에서 표현하는 문자)와 유니코드
		 * 
		 * char(캐릭터) 문자형 또는 정수형 - 문자 또는 기호 하나만을 저장하는 형식
		 * 
		 * boole(참과 거짓을 표현하는 불) - 참, 거짓을 저장 1바이트 
		 *
		 * String (문자형의 집합이고 문자열이라는 데이터 형식은 따로 존재하지 않음) (문자열 입력한 글자 수x2) 바이트
		 *
		 */
		
		
		
		
		
	}
}


